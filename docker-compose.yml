version: '3.8'
services:
  app:
    build: .
    container_name: docker-23P31A05H8
    ports:
      - "8080:8080"
    volumes:
      - seed-data:/data
      - cron-output:/cron
    environment:
      - TZ=UTC
    restart: unless-stopped
volumes:
  seed-data:
  cron-output:
